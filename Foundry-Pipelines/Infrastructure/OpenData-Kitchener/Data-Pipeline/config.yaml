gateways:
  local:
    connection:
      type: duckdb
      database: ../../../../db/metricforge.duckdb
    # In Docker / CI the state backend is PostgreSQL:
    # state_connection:
    #   type: postgres
    #   host: {{env_var('SQLMESH_POSTGRES_HOST')}}
    #   port: {{env_var('SQLMESH_POSTGRES_PORT')}}
    #   user: {{env_var('SQLMESH_POSTGRES_USER')}}
    #   password: {{env_var('SQLMESH_POSTGRES_PASSWORD')}}
    #   database: {{env_var('SQLMESH_POSTGRES_DATABASE')}}

default_gateway: local
default_target_environment: local

model_defaults:
  dialect: duckdb
  start: '2020-01-01'
  cron: '@daily'

linter:
  enabled: true
  rules:
    - ambiguousorinvalidcolumn
    - invalidselectstarexpansion
